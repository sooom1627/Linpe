import { View } from "react-native";

/**
 * StatCardコンポーネントのスケルトンローディング表示
 * 抽象的な表現でローディング状態を示す
 */
export const StatCardSkeleton = () => {
  return <View className="h-20 flex-1 rounded-lg bg-zinc-50 p-3"></View>;
};
